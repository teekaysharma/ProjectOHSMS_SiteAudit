# Handover to the desktop session, 2026-09-25

The cloud session that did this work is ending. Everything it produced is on branch
`claude/gracious-fermat-0gke4a`, draft PR
https://github.com/teekaysharma/ProjectOHSMS_SiteAudit/pull/29. Nothing is kept anywhere
else: the cloud scratchpad (downloaded PDFs, text extracts, diffs) is lost when the
session ends.

## Start here

1. `git fetch origin && git checkout claude/gracious-fermat-0gke4a`
2. `npm ci`, then `npm test` (expect `# pass 11`, `# fail 0`), `npm run build`,
   `npm run build:app`.
3. Read `CLAUDE.md`, then `intent/0001-local-first-rearchitecture/` (`intent.md`,
   `spec.md`, `plan.md`), then `docs/references.md`.

## State

- **Nothing is built beyond Stage 0.** `app/` is a placeholder. Stages 1 to 5 wait for the
  owner to correct and accept `intent.md` and accept `spec.md` (CLAUDE.md: never
  implement without an accepted plan).
- **Spec concerns C1 to C14** in `spec.md` "Flagged concerns" are open owner decisions.
  They are not to be settled or built around by the implementer.
- **Sources read:** every document in `docs/references.md` "Received and read in full".
  Public ADPHC and federal texts are in `docs/sources/` (verbatim; never edit). ISO
  standards, ADIBC 2013 and the UAE Fire and Life Safety Code 2018 are cited by clause
  only: they are licensed or copyright-reserved. The owner holds those files locally.
- **PR 29:** draft, CI green, no review comments, mergeable. Its description was
  refreshed on 2026-09-25. The cloud PR watch and hourly check-ins are stopped; watch the
  PR from the desktop if needed.

## Waiting on the owner

1. **Review** `intent/0001-local-first-rearchitecture/r1a-score-definitions-DRAFT.md`
   (answers R1a; section 5 lists the five questions to answer). Accepted text then goes
   into `spec.md` C7 and C8.
2. **R5:** the VisualRiskAssessor report disclaimer text (spec C3).
3. **Accept or correct** `intent.md` and `spec.md`, including decisions on C1 to C14.
4. **Git history:** commit `527985f` still contains an extract of the fire code's
   Chapters 4 and 5, removed from the tree in `d60f4b1`. Purging it needs a history
   rewrite and force-push of this branch; only on the owner's instruction.

Optional, not blocking: the primary Abu Dhabi municipal rule for temporary site fences
(R35 is a news source only); any MoIAT amendment of the 2016 PPE annex.

## After acceptance

Write `plans/1-*.md` for Stage 1 with Superpowers writing-plans, get it accepted, then
execute with TDD. Every number that reaches a report needs a test, band boundaries
included; bands are strict `>` on the unrounded percentage; score 0 is excluded from
averages. See CLAUDE.md "Safety posture" and "Rules that protect the evidence".
